/*
 * Test Store.
 * @author Frank Wood
 */

describe('Test Suite For VlanStore', function() {

    // var vlanArray = [ 'vlan111', 'vlan222', 'vlan333' ],
    //     testResponses = {
    //         vlans: {
    //             success: {
    //                 status: 200,
    //                 responseText: JSON.stringify({ vlans: vlanArray })
    //             }
    //         }
    //     },
    //     VlanStore,
    //     VlanActions;
    //
    // beforeEach(function() {
    //     VlanStore = require('VlanStore');
    //     VlanActions = require('VlanActions');
    // });
    //
    // it('has no data at the start', function() {
    //     expect(VlanStore.vlans.length).toEqual(0);
    // });
    //
    // it('has data after the load action', function() {
    //     VlanActions.loadVlans();
    //     jasmine.clock().tick(); // allow action
    //
    //     AjaxRespond(testResponses.vlans.success);
    //     jasmine.clock().tick(); // allow action
    //
    //     expect(VlanStore.vlans.length).toEqual(3);
    //     expect(VlanStore.vlans).toEqual(vlanArray);
    // });
    //
    // it('has no data at the start (again)', function() {
    //     expect(VlanStore.vlans.length).toEqual(0);
    // });

});
